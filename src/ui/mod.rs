pub mod layout;
pub mod document;
pub mod capture;
pub mod calendar;

pub fn render(frame: &mut ratatui::Frame, app: &crate::app::state::AppState) {
    // Stub — will be implemented in Task 15
    let _ = (frame, app);
}
